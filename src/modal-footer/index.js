import ModalFooter from '../modal/src/modal-footer';

/* istanbul ignore next */
ModalFooter.install = function (Vue) {
  Vue.component(ModalFooter.name, ModalFooter);
};

export default ModalFooter;
