import Icon from './src/icon.vue';
import transformVueComponent from '../utils/sfc';

/* istanbul ignore next */
const IconComponentOptions = transformVueComponent(Icon);

export default IconComponentOptions;
